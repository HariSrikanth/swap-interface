// Entire file commented out temporarily to debug build error
/*
import { type ReactNode } from "react";

// Temporarily disabled tRPC setup to fix build
export const api = {
  createClient: () => ({}),
  Provider: ({ children }: { children: ReactNode }) => <>{children}</>,
};
*/ 